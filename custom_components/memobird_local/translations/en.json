{
  "config": {
    "step": {
      "user": {
        "title": "Memobird printer",
        "description": "Local network address of the printer. Give it a fixed IP in your router.",
        "data": {
          "host": "Host",
          "name": "Name"
        }
      }
    },
    "error": {
      "cannot_connect": "Can't reach the printer at this address."
    },
    "abort": {
      "already_configured": "This printer is already configured."
    }
  },
  "services": {
    "print": {
      "name": "Print",
      "description": "Print text on the Memobird printer.",
      "fields": {
        "message": {
          "name": "Message",
          "description": "Text to print. Characters outside GBK (e.g. emoji) print as '?'."
        },
        "title": {
          "name": "Title",
          "description": "Large bold heading."
        },
        "big": {
          "name": "Large font",
          "description": "Print the message in the large font."
        },
        "bold": {
          "name": "Bold",
          "description": "Print the message in bold."
        },
        "underline": {
          "name": "Underline",
          "description": "Underline the message."
        },
        "timestamp": {
          "name": "Timestamp",
          "description": "Print the current date and time under the title."
        },
        "separator": {
          "name": "Separators",
          "description": "Print rules around the message."
        },
        "format": {
          "name": "Format",
          "description": "`plain`, `markdown` (`**bold**`, `*italic*`, `++underline++`, `~~strike~~`, `` `code` ``, `# heading`, `- list`) or `html` (tags b, i, u, s, code, big, small, h1-h3, center, ul/ol/li, hr, br). Defaults to the integration options."
        },
        "font_size": {
          "name": "Font size",
          "description": "Base font size in pixels for markdown and html, 12-64. Defaults to the integration options."
        }
      }
    },
    "print_image": {
      "name": "Print image",
      "description": "Print an image from a file, a URL, a camera snapshot or SVG markup. It is scaled to the paper width (384 dots) and converted to black and white.",
      "fields": {
        "file": {
          "name": "File",
          "description": "Local path, e.g. /config/www/photo.jpg. Must be in allowlist_external_dirs."
        },
        "url": {
          "name": "URL",
          "description": "Image to download."
        },
        "camera": {
          "name": "Camera",
          "description": "Camera entity to take a snapshot from."
        },
        "svg": {
          "name": "SVG",
          "description": "SVG markup to print, e.g. line art from ai_task.generate_data. Shapes, paths, transforms, fills and strokes are supported; text and gradients are ignored."
        },
        "title": {
          "name": "Title",
          "description": "Large bold heading above the image."
        },
        "caption": {
          "name": "Caption",
          "description": "Text printed under the image."
        },
        "dither": {
          "name": "Dithering",
          "description": "Simulate grey shades with dots (good for photos). Turn off for logos, QR codes and line art. SVG is printed without dithering unless this is on."
        },
        "timestamp": {
          "name": "Timestamp",
          "description": "Print the current date and time under the title."
        },
        "separator": {
          "name": "Separators",
          "description": "Print rules around the image."
        },
        "format": {
          "name": "Format",
          "description": "Caption format: `plain`, `markdown` (`**bold**`, `*italic*`, `++underline++`, `~~strike~~`, `` `code` ``, `# heading`, `- list`) or `html` (tags b, i, u, s, code, big, small, h1-h3, center, ul/ol/li, hr, br). Defaults to the integration options."
        },
        "font_size": {
          "name": "Font size",
          "description": "Base font size in pixels for markdown and html, 12-64. Defaults to the integration options."
        }
      }
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Memobird options",
        "description": "Markdown and HTML are rendered as an image with bundled fonts, so single words can be bold, italic, underlined or struck through. Plain text uses the printer's built-in font.",
        "data": {
          "default_format": "Default text format",
          "font_size": "Font size"
        },
        "data_description": {
          "default_format": "Used by notify.send_message and when an action doesn't set format.",
          "font_size": "Base size for Markdown and HTML text (24 px by default). Headings scale with it. Plain text uses the printer's built-in font and isn't affected."
        }
      }
    }
  },
  "selector": {
    "format": {
      "options": {
        "plain": "Plain text",
        "markdown": "Markdown",
        "html": "HTML"
      }
    }
  }
}